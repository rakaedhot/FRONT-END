import React from "react";

// Web fallback: react-native-maps is native-only. The Absen screen renders its
// own styled placeholder on web, so this stub simply renders nothing.
export default function MapBox(_props: any) {
  return null;
}
